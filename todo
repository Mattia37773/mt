# endto end tests: make test TEST=tests

- singles
  - npm
  - config
  - update
  - yarn

---

- test
  - all

- e2e
  - mutliple test profile

- external
  - all with already compiled binary

- also make error test for when it should failt
- remove requirement for .mt.yaml file

- run command can execeute command inside a shell with a specific user
- main container in config
- rework test entirely so they check only the builded thing and not the execeution itself
- interactive cofnig create command
