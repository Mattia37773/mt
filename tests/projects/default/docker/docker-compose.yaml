name: ${COMPOSE_DEAULT_NAME}
services:
  db:
    container_name: ${COMPOSE_DEAULT_NAME}-fpm
    image: debian:bookworm
    restart: always
    command: sleep infinity
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
